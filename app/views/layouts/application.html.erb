<!DOCTYPE html>
<html>
<head>
  <title>Lincoln Douglas Debate</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= csrf_meta_tags %>

  <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="/assets/html5shiv.js"></script>
      <script src="/assets/twitter/bootstrap/respond.min.js"></script>
    <![endif]-->


</head>
<body style screen_captured_injected="true">

    <%= render 'navbar2'%>



    <% if current_user %>
        <%= render 'sidebar' %>
    <% end %>

    <div class="content">
      <% unless flash.blank? %>
          <div class="row">
            <div class="col-md-12">
              <% flash.each do |key, value| %>
                  <div class='alert alert-info' id='<%= key %>'>
                    <%= value %>
                  </div>
              <% end %>
            </div>
          </div>
      <% end %>
      <%= yield %>
    </div>

  <footer class="col-lg-12">
        <small>© 2013 <a href="http://amyhua.me/">Amy Hua</a> & <a href="http://leandratejedor.com/">Leandra Tejedor</a>
      </footer>

  <!-- Place Javascript last so the pages load faster -->
  <%= javascript_include_tag "application" %>



</body>
</html>
