<div id="case">
	<h2><%= @case.title %></h2>
	<%= link_to "Edit Case", [:edit, @resolution, @case] %><br>
	<%= link_to "Delete Case", [@resolution, @case], :method => :delete, :confirm => "Are you sure you want to delete this case?" %><br>
	<%= @case.content %>
</div>