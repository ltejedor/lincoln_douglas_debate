<div class="container col-sm-12">

<h2><%= @resolution.name %></h2>

	<% if @resolution.current == true %>
	<p><b>This is the Current Resolution</b><p>
	<% end %>
	<a class="btn btn-large btn-primary create-tournament tournament-btn"<%= link_to "Submit Case for Review", new_resolution_case_path(@resolution) %></button><br>

	<ul id="cases"
		<% @resolution.cases.each do |c| %>
			<li>
				<%= link_to c.title, [@resolution, c] %>
				<%= c.description %>
			</li>
		<% end %>
	</ul>
	<br>


<%= link_to "Edit Resolution", edit_resolution_path(@resolution) %> or

<%= link_to "Delete Resolution",
	resolution_path(@resolution),
	:method => :delete,
	:confirm => "Are you sure you want to delete this debate topic?" %><br><%= link_to "Back to All Resolutions", resolutions_path %>

</div>