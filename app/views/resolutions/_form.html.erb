<%= simple_form_for [@resolution] do |f| %>
	<%= f.error_messages %>

		<%= f.input :name %><br>

		<%= f.input :description %><br>

		<%= f.input :current, label: 'This is the current resolution.' %><br>


		<br>
	<%= f.button :submit %>

<% end %>
<br><br>